{
  "name": "hihi",
  "version": "0.0.1",
  "type": "module",
  "main": "src/index.ts",
  "license": "ISCC",
  "scripts": {
    "init": "pnpm nx pre-init",
    "start-billing-api": "TEST PUBLIC RUN",
    "create-workflow": "make -C workflows poetry-install helm-build"
  },
  "dependencies": {
  
  }
}
